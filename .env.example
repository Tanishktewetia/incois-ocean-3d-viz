# Leave empty when the frontend and API are deployed together on Vercel.
# Set this to the public URL of a separate data-backed FastAPI deployment
# when NetCDF files are not included in the serverless bundle.
VITE_API_BASE_URL=

# Backend-only settings for the Render deployment.
SUPABASE_URL=https://wblagmylaxfsqgwxaoch.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
FRONTEND_ORIGINS=https://oceanscope-eight.vercel.app