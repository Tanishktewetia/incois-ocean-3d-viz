{
  "framework": null,
  "buildCommand": "npm ci --prefix frontend && npm run build --prefix frontend -- --outDir ../public",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/((?!api(?:/|$)|health$).*)", "destination": "/index.html" }]
}